<h1>Editing campaign</h1>

<%= error_messages_for :campaign %>

<% form_for(@campaign, :url => organization_segment_campaign_path(@organization, @segment ), :method => :put ) do |f| %>

   <%= render :partial => 'fields', :locals => {:f => f, :organization => @organization} -%>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', organization_segment_campaign_path(@organization, @segment, @campaign) -%>
|
 <%= link_to 'Back', organization_segment_campaigns_path(@organization, @segment ) %>
