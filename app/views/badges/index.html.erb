<h1>Badge Catalog</h1>

<ul>
<% @badges.each do |badge| %>

<li><%= link_to badge.name, :controller => 'badges' , :action => 'show'  -%> </li>

<% end %>
</ul>

<% if false %>
TODO: DELETE THIS SCAFFOLD  CODE ONCE THE LGOIC IS CLEAR
<table>
  <tr>
  </tr>

<% for badge in @badges %>
  <tr>
    <td><%= link_to 'Show', badge %></td>
    <td><%= link_to 'Edit', edit_badge_path(badge) %></td>
    <td><%= link_to 'Destroy', badge, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New badge', new_badge_path %>
<% end %>
