<h1>Editing badge</h1>

<%= error_messages_for :badge %>

<% form_for(@badge, :url => organization_segment_badge_path(@organization, @segment ), :method => :put ) do |f| %>

<%= render :partial => 'fields', :locals => {:f => f } %>

<p>
 <%= f.submit "Update" %>
</p>

<% end %>

<%= link_to 'Show', organization_segment_badges_path(@organization,@segment) %> 
|
<%= link_to 'Back', organization_segment_badges_path(@organization, @segment) %>
