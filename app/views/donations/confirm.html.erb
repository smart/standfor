<% content_box("Confirm your Payment Details", :id => "payment", :class => "shadow") do %>

<p>I allow my card to be charged $<%= @current_donation.amount -%> </p>

<%= form_tag({:controller => 'donations', :action => 'create'}, {:id => 'confirm-form'})   %>
  <%= hidden_field_tag  'confirmed', 'yes' %>
  
  <%= button(:image => "labels/confirm.png", :alt => "confirm", 
	:class => "large-button", :form_id => "confirm-form", :type => 'submit' ) -%>
</form>

<% end %>
