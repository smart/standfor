<% Sponsor.find(:all, :conditions => 'featured = 1', :order => 'RAND()', :limit => 1).each do |sponsor| %>
	<%= link_to image_tag(sponsor.sponsorslogo.public_filename(:thumb)), sponsor_path(sponsor) -%>
  <%= sponsor.name -%>
<% end %>
