<h1>Editing badge</h1>

<%= error_messages_for :badge %>

<% form_for(@badge) do |f| %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @badge %> |
<%= link_to 'Back', badges_path %>
