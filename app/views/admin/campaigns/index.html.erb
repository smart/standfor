<h1>Listing campaigns</h1>

<table>
  <tr>
  </tr>

<% for campaign in @campaigns %>
  <tr>
    <td><%= link_to 'Show', admin_organization_campaign_path(@organization, campaign ) %></td>
    <td><%= link_to 'Edit', edit_admin_organization_campaign_path(@organization, campaign) %></td>
    <td><%= link_to 'Destroy', admin_organization_campaign_path(@organization, campaign), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New campaign', new_admin_organization_campaign_path(@organization) %>
