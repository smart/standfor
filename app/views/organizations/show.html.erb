<ul>
<% @organization.segments.each do |segment| %>

<li><%= link_to segment.name, :controller => 'segments', :action => 'show', :segment => segment.site_name, :organization => @organization.site_name -%></li>

<% end %>
</ul>
