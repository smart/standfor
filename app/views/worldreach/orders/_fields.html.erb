<ul>
 <% organization.segments.each do |segment| %> 
  <% if !session[:causes][segment.site_name].nil? %>
   <li>
     <%= segment.name -%> : 
     <input type="text"  size="5" name="segment[<%=segment.site_name-%>]" 
      value="<%= current_donation_value(@order, segment) -%>" > 
    </li>
   <% end %>
 <% end %>
</ul>
