<% form_for(order, :url => worldreach_orders_path ) do |f| %>
  <%= render :partial => '/worldreach/orders/fields', :locals => { :f => f , :organization => organization } -%>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

