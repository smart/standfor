<h1>Editing donation</h1>
<%= error_messages_for :donation %>
<% form_for(:donation, :url =>  organization_segment_donations_path(@organization, @segment), :method => :post ) do |f| %>
 <%= render :partial => 'fields', :locals => {:f => f} -%>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', organization_segment_donations_path(@organization, @segment ) -%>
<%= link_to 'Back', organization_segment_donations_path %>
